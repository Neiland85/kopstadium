# PUERTO donde corre el backend
PORT=3000

# URL de la base de datos PostgreSQL
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://postgres:postgres@db:5432/kopstadium

# URL de Redis (para cache y colas)
# Formato: redis://HOST:PORT
REDIS_URL=redis://redis:6379

# Clave secreta para firmar JWT
JWT_SECRET=tu_secreto_muy_seguro_aqui

# Entorno (development | production | test)
NODE_ENV=development

# Opcional: configuración de correo
EMAIL_HOST=smtp.example.com
EMAIL_PORT=587
EMAIL_USER=usuario@example.com
EMAIL_PASS=secretpassword

# Opcional: API keys de servicios externos

